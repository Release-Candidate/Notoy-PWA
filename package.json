{
    "targets": {
        "app": {
            "context": "browser",
            "source": "./http/index.html",
            "distDir": "./dist/"
        }
    },
    "browserslist": "> 0.5%, last 2 versions, not dead",
    "scripts": {
        "list": "gulp --tasks",
        "clean": "gulp clean",
        "install": "spago install",
        "build": "gulp bundle",
        "test": "spago test",
        "repl": "spago repl",
        "bundle": "gulp bundle && parcel build http/index.html --public-url ./",
        "serve": "gulp serve"
    },
    "devDependencies": {
        "@parcel/packager-raw-url": "^2.0.1",
        "@parcel/service-worker": "^2.0.1",
        "@parcel/transformer-webmanifest": "^2.0.1",
        "delete": "^1.1.0",
        "eslint": "^8.3.0",
        "gulp": "^4.0.2",
        "gulp-cli": "^2.3.0",
        "gulp-connect": "^5.7.0",
        "gulp-string-replace": "^1.1.2",
        "parcel": "^2.0.1",
        "spago": "^0.20.3"
    }
}
